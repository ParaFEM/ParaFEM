p129.example.tar.gz contains an example input deck to use with program p129
in ParaFEM.

To unzip, type:

$gunzip p129.example.tar.gz

To extract the files from the archive, type:

$tar xvf p129.example.tar
