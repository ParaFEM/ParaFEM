# MAKEFILE src/tools/preprocessing/ed4mesher/makeprograms.ppc970
# Author: Lee Margetts
# Modified by: Louise M. Lever
# -----------------------------------------------------------------------
# Compiles sg12mg program executable as RELEASE or DEBUG
# Links with RELEASE STATIC library libParaFEM_mpi.$(VERSION).a
#    or with RELEASE STATIC library libParaFEM_mpi_D.$(VERSION).a
# -----------------------------------------------------------------------
# Available Rules:
#   (default) : execute the release rule
#
#   release: Build sg12mg executable as RELEASE edition
#   debug: Build sg12mg executable as DEBUG edition
#   install: Copy sg12mg executable into global bin directory
#   clean: Remove .o object files
#   execlean: Remove the local sg12mg executable
#   relink: Rebuild sg12mg from object files and libraries
# -----------------------------------------------------------------------
# Notes:
# -----------------------------------------------------------------------

include ../../../../include/mk_defs.inc

MODULE	= ../../../../include
LIB	= ../../../../lib
FC=mpif90
.SUFFIXES: .o .f90

all:	release

release:
	$(MAKE) -f makeprograms.ppc970 sg12mg \
	FFLAGS="-c -I$(MODULE) -O3 -qstrict -qtune=ppc970 -qarch=ppc970 -qcache=auto -q64" \
	LDFLAGS="-L$(LIB) -lParaFEM_mpi.$(VERSION)"

debug:
	$(MAKE) -f makeprograms.ppc970 sg12mg \
	FFLAGS="-c -I$(MODULE) -V -g" \
	LDFLAGS="-L$(LIB) -lParaFEM_mpi_D.$(VERSION)"

sg12mg: sg12mg.o
	$(FC) sg12mg.o -o sg12mg $(LDFLAGS)

sg12mg.o: sg12mg.f90
	$(FC) sg12mg.f90 $(FFLAGS)

clean:
	rm -f *.o

execlean:
	rm -f sg12mg

install:
	rm -f *.o
	rm -f *.mod
	mv sg12mg ../../../../bin

relink: sg12mg

